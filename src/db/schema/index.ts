// No domain tables yet — this file exists so the Drizzle migration
// workflow (drizzle.config.ts, drizzle-kit generate/migrate) has a schema
// entry point to point at. Domain tables land in later tasks.
export {};
